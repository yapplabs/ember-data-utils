import parseQueryToFunction from 'dummy/utils/parse-query-to-function';
import { module, test } from 'qunit';

module('Unit | Utility | parse-query-to-function', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = parseQueryToFunction();
    assert.ok(result);
  });
});
